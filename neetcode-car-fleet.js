class Solution {
    /**
     * @param {number} target
     * @param {number[]} position
     * @param {number[]} speed
     * @return {number}
     */
    carFleet(target, position, speed) {
        console.log(target, position, speed)
        let fleet = 0;
        let max = position.length * target;
        while(position);
            position.map(speed);
        }
    return fleet;
}

const t = new Solution();
console.log(t.carFleet(10, [1, 4], [2, 1]));